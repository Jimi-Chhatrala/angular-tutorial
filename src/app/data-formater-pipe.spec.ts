import { DataFormaterPipe } from './data-formater-pipe';

describe('DataFormaterPipe', () => {
  it('create an instance', () => {
    const pipe = new DataFormaterPipe();
    expect(pipe).toBeTruthy();
  });
});
