import { ExampleDirectory } from './example-directory';

describe('ExampleDirectory', () => {
  it('should create an instance', () => {
    const directive = new ExampleDirectory();
    expect(directive).toBeTruthy();
  });
});
