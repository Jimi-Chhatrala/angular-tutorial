import { CurrencyUsdToInrConverterPipe } from './currency-usd-to-inr-converter-pipe';

describe('CurrencyUsdToInrConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyUsdToInrConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
